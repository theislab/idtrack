comment: false
coverage:
    status:
        project:
            default:
                # Keep Codecov's UI/status aligned with the CI gate (see `fail_under` in `pyproject.toml`).
                target: 60%
                threshold: 0%
                # if_ci_failed: success #success, failure, error, ignore
        patch:
            default:
                target: 60%
                threshold: 0%
                # if_ci_failed: success #success, failure, error, ignore
