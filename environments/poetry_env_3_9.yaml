# IDTrack Development and Reproducibility Environment 

name: poetry_env_3_9

channels:
  - conda-forge

dependencies:
  - python=3.9
  - jupyterlab
  - pandoc
  - hdf5
  - poetry
  - pip:
    - cookietemple

# apt-get/brew install pandoc